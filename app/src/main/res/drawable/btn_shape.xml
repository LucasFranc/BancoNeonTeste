<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/strong_cyan">
    <item>
        <shape
            android:shape="rectangle"
            android:padding="10dp">
            <solid android:color="@color/strong_cyan"/>
            <corners
                android:bottomRightRadius="@dimen/btn_shape_size"
                android:bottomLeftRadius="@dimen/btn_shape_size"
                android:topLeftRadius="@dimen/btn_shape_size"
                android:topRightRadius="@dimen/btn_shape_size" />
        </shape>
    </item>
</ripple>